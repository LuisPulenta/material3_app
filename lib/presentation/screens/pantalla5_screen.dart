import 'package:flutter/material.dart';

class Pantalla5Screen extends StatelessWidget {
  const Pantalla5Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pantalla5Screen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('Pantalla5Screen'),
      ),
    );
  }
}
